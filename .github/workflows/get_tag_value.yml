name: Get tag

on:
  create:
    tags:
      'release-gcp-*-*.*.*'
env:
  TAG: ${GITHUB_REF:10}


jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get Tag and Version
        run: |
          TAG=${GITHUB_REF:10}
          ENVIRONMENT=$(echo "$TAG"|cut -d\- -f3)
          RELEASE_VERSION=$(echo "$TAG"|cut -d\- -f4)
      - name: If Dev
        if: env.TAG == 'release-gcp-1.1.3'
        run: echo "qa"
      - name: If Version
        if: env.TAG == 'qa'
        run: echo "qa"